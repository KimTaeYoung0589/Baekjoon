/* 색상환 */
#include <iostream>
using namespace std;

int dp[1001];
int n, k;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    
    cin >> n >> k;
    dp[0] = 0;
    dp[1] = n;
    dp[2] = n / 2;

    for (int i = 0; i < n; i++){
        
    }
        return 0;
}