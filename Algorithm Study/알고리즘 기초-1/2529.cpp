// 부등호
#include <iostream>
using namespace std;

int k;

int main(){
    ios_base::sync_with_stdio(false);
    cout.tie(nullptr);
    cin.tie(nullptr);

    cin >> k;
    for (int i = 0; i < n; i++){
        
    }
        return 0;
}